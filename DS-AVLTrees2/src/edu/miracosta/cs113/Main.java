package edu.miracosta.cs113;
import java.text.DecimalFormat;

public class Main {
	
	public static void main(String[] args)
	{
		/**
		 * Create a driver that insert a collection of more than 20 randomly 
		 * generated numbers. Insert the same set of numbers in a binary search 
		 * tree that is not balanced. Verify that each tree is correct by 
		 * performing an in-order traversal. Also, display the format of each 
		 * tree that was built and compare their heights.
		 */
		
		
		/*
		StringBuilder mainSB = new StringBuilder();
		BinarySearchTree<Double> btTest = new BinarySearchTree<Double>();
		AVLTree<Double> test = new AVLTree<Double>();
		
		for(int i = 0; i < 20; i++)
		{
			double randInt = Math.random() * 100;
			test.add(randInt);
			btTest.add(randInt);
		}
		System.out.println(test.toString());
		System.out.println(btTest.toString());
		
		System.out.println(test.toString2());
		System.out.println(btTest.toString2());
		*/
		
	}

}
